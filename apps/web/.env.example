# Agent Mail (HTTP MCP endpoint)
AGENT_MAIL_BASE_URL=http://127.0.0.1:8765
AGENT_MAIL_PATH=/mcp/
# AGENT_MAIL_BEARER_TOKEN=

# Brenner Bot defaults
BRENNER_PROJECT_KEY=/data/projects/brenner_bot
AGENT_NAME=GreenCastle

# Optional: override the public origin used for server-side corpus fetch fallbacks.
# If unset, the app uses `VERCEL_URL` (when present) or falls back to https://brennerbot.org.
# BRENNER_PUBLIC_BASE_URL=http://localhost:3000

# Lab mode gating (fail-closed). Set to 1 to enable orchestration routes/actions.
BRENNER_LAB_MODE=0

# Optional app-layer shared secret for lab mode (recommended for local dev and as a fallback).
# Provide via `x-brenner-lab-secret` header or `brenner_lab_secret` cookie.
# When deployed behind Cloudflare Access, you can also allow Cloudflare Access headers
# by setting BRENNER_TRUST_CF_ACCESS_HEADERS=1 (only if the origin is not reachable directly).
BRENNER_LAB_SECRET=

# Trust Cloudflare Access headers for lab mode auth (disabled by default; set to 1 only when behind CF Access).
BRENNER_TRUST_CF_ACCESS_HEADERS=0

# Optional shared secret for defense-in-depth (app-layer auth fallback).
# When set, requests to /sessions/* must include this secret via:
#   - Header: x-brenner-lab-secret
#   - Cookie: brenner_lab_secret
# Leave empty to rely solely on BRENNER_LAB_MODE + Cloudflare Access.
# BRENNER_LAB_SECRET=your-secret-here
